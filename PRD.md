# 体重记录 App PRD（个人本地使用）

> 版本：v0.1（需求确认稿）
> 更新日期：2026-08-31
> 本文档是后续开发的唯一依据，开发前以本文为准。

## 1. 项目概述

- 一款**只供自己使用**的体重记录 App，用 Flutter + Android Studio 开发。
- **仅在本地运行**：数据保存在当前电脑/本机，刷新、关闭、重新启动后不丢失。
- **不需要**：登录、联网、云数据库、部署、多端同步。
- 运行目标：本机 Android 模拟器或真机（Flutter 构建产物），不依赖任何网络服务。
- 项目目录：本仓库根目录（Flutter 工程，含 pubspec.yaml）
  - 说明：正式开发前已通过 `flutter create` 补全 Flutter 工程结构。

## 2. 技术方向（第一版）

- 框架：Flutter（本地安装的 SDK）
- 开发工具：Android Studio
- 本地存储方案（方向，具体实现时确定）：
  - 体重记录：本地数据库（如 SQLite / sqflite）
  - 照片：复制保存到 App 本地文件目录，数据库只存文件路径
- 折线图：使用 Flutter 图表库（如 fl_chart）

## 3. 功能需求

### 3.1 记录体重（含时间）

- 在"我的"页面进入"记录"功能。
- 输入体重，单位为**公斤（kg）**，建议保留 1 位小数。
- 自动记录**当时的日期和时间**，无需手动填写。

### 3.2 上传照片（可选）

- 记录体重时，可选择**拍照或从相册选择一张照片**一并保存；也可以不选照片。
- **新增需求：上传时可选是否压缩**：
  - 选"压缩"：保存压缩后的图片（体积小，画质略有损失）。
  - 选"不压缩"：保存原图。
  - 默认建议为"压缩"（实现时确定，可后续调整）。

### 3.3 查看过往记录（含增删）

- 在"我的"页面进入"查看记录"功能。
- 以列表形式展示所有历史记录：时间、体重（kg）、是否带照片。
- 支持**新增**（回到记录页）、**删除**（删除记录，对应照片一并删除或保留由实现时决定，默认一并删除）。

### 3.4 折线图（首页核心内容）

- 展示所有已有体重记录。
- X 轴：时间；Y 轴：体重（公斤 kg）。
- 数据来自本地数据库，随记录增删实时刷新。

### 3.5 照片对比（查看变化）

- 将带照片的记录按**时间顺序**排列。
- 支持**左右滑动切换/播放**进行前后对比。
- 通过首页"查看变化"按钮进入该页面。

## 4. 页面结构

- **首页**：
  - 核心内容：折线图（功能 3.4）
  - "查看变化"按钮（点击跳转到照片对比页）
  - 底部导航栏："首页" / "我的"
- **我的**：
  - "记录"入口（功能 3.1 + 3.2）
  - "查看记录"入口（功能 3.3）
- **照片对比页**（从首页按钮跳转，功能 3.5）

## 5. 第一版范围

### 5.1 第一版要做

- 记录体重（kg）和当时时间
- 记录时可选带照片，且可选是否压缩
- 历史记录列表：查看、新增、删除
- 体重-时间折线图（首页）
- 照片按时间顺序滑动对比（首页入口）
- 底部导航：首页 / 我的
- 本地持久化存储（关闭重启不丢数据）

### 5.2 第一版不做

- 编辑已有记录（改体重、换照片；需要时先删后重新记录）
- 目标体重、计划、提醒
- 登录、联网、云数据库、多端同步、部署
- 数据导出/备份（可后续增加）
- BMI、变化量等统计指标（可后续增加）
- 多张照片/单条多图（每条记录第一版仅 1 张照片）

## 6. 需求确认记录

- 2026-08-31：确认体重单位使用**公斤（kg）**。
- 2026-08-31：新增需求——**上传图片时可选是否压缩**。
- 待细化（开发时定，不需要用户回答）：照片压缩默认值、删除记录时照片是否一并删除。